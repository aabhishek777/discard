import { Explore } from "@mui/icons-material";
import { Button } from "@mui/material";
import React from "react";

const SidebarCompassIcon = () => {
  return (
    <div>
      <Button>
        <Explore />
      </Button>
    </div>
  );
};

export default SidebarCompassIcon;
