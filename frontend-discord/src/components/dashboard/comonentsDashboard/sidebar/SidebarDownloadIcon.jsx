import { Download } from "@mui/icons-material";
import { Button } from "@mui/material";
import React from "react";

const SidebarDownloadIcon = () => {
  return (
    <div>
      <Button>
        <Download />
      </Button>
    </div>
  );
};

export default SidebarDownloadIcon;
